import { TenantYonetimiRoutes } from './tenant-yonetimi.routes';

describe('TenantYonetimiRoutes', () => {
  it('should create an instance', () => {
    expect(new TenantYonetimiRoutes()).toBeTruthy();
  });
});
